apiVersion: v1
kind: Service
metadata:
  name: gitops-app-service
  namespace: default
  labels:
    app: gitops-app
spec:
  type: LoadBalancer
  selector:
    app: gitops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
